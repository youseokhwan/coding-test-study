fun main() {
    val (a, b) = readLine()!!.split(" ")
    println(a.toInt() + b.toInt())
    println(a.toInt() - b.toInt())
    println(a.toInt() * b.toInt())
    println(a.toInt() / b.toInt())
    println(a.toInt() % b.toInt())
}
